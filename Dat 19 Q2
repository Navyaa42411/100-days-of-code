#include<stdio.h>
int main(){
    int a;
    printf("Enter the number you want the sum of digits of :");
    scanf("%d",&a);
    int sum = 0;
    while(a > 0){
        sum += a % 10;
        a /= 10;
    }
    printf("The sum of digits is : %d",sum);
    return 0;
}
